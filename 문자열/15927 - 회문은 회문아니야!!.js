const fs = require('fs');
const input = fs
  .readFileSync('./문자열/15927input.txt')
  .toString()
  .trim()
  .split('\r\n');
// const input = fs.readFileSync('/dev/stdin').toString().trim().split('\n');
